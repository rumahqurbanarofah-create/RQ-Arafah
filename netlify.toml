# Netlify Configuration File
# Configured specifically for Vite + React Single Page Application (SPA)

[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
